var brick = require('.')

brick.defaults.builder = 'pg'

var conditions = brick.conditions({
  color: 'Red',
  city: brick('city IS NULL'),
  test: brick('count > ?', 4)
})

console.log('conditions', conditions.toString())
